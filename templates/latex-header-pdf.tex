% DOCUMENT COMPOSITION DIRECTIVES (PDF)

% Use PDF images
\newcommand{\imagesuffix}{}

% Show source only if not empty
\newcommand{\source}[1]{
  \def\temp{#1}\ifx\temp\empty
  \else
    [#1]
  \fi
}

% Display problems + solutions
\newcommand{\problem}[4]{
  \stepcounter{problem}
  \begin{tcolorbox}[breakable,notitle,boxrule=0pt,colback=light-gray,colframe=light-gray]
    \textbf{Úloha \theseminar .\theproblem.}
    \source{#1} #3
  \end{tcolorbox}
  \noindent#4
}

\newcommand{\home}[1]{
  \subsection*{Domáca práca}

  #1}
\newcommand{\teachernote}[1]{#1}

% bibliography management
\usepackage[
    backend=biber,      % use biber as backend instead of BiBTeX
    style=alphabetic,   % citation style
    url=true,           % display urls in bibliography
    hyperref=auto,      % detect hyperref and create links
    giveninits=true,    % abbreviate first names to initials
    maxbibnames=5,      % maxiumim number of authors before making 'et al.'
    alldates=iso,   % set date format to ISO 8601
    seconds=true
]{biblatex}
\addbibresource{bibliografia.bib}